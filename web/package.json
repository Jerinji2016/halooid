{
  "name": "halooid-web",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "echo \"Please run dev in the specific app directory\" && exit 1",
    "build": "echo \"Please run build in the specific app directory\" && exit 1",
    "storybook": "echo \"Please run storybook in the specific package directory\" && exit 1"
  },
  "devDependencies": {
    "@sveltejs/kit": "^1.20.0",
    "@sveltejs/vite-plugin-svelte": "^2.0.0",
    "svelte": "^3.58.0",
    "vite": "^4.3.0",
    "eslint": "^8.40.0",
    "eslint-plugin-svelte": "^2.30.0",
    "prettier": "^2.8.0",
    "prettier-plugin-svelte": "^2.10.0"
  }
}
